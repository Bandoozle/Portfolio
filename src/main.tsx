import React from 'react'
import ReactDOM from 'react-dom/client'
import PortfolioRoot from './PortfolioRoot.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PortfolioRoot />
  </React.StrictMode>,
)

